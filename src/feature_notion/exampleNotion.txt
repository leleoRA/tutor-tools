- Thiago Paiva
	### Semana #17-Sing me a Song
	- **Feedback de Entrega** 
		- Avaliação geral: `Acima das expectativas`
		- Quais foram os requisitos avaliados nesse projeto?
			- POST /recommendations
				- Cria nova recomendação de acordo com os dados enviados em caso de sucesso?
			- POST /recommendations validação
				- É possível se logar no site?
			- POST /recommendations/:id/upvote ou downvote
				- Adiciona ou remove um ponto a uma recomendação?
				
			- POST /recommendations/:id/downvote: Exclusão
				- Exclui a recomendação se ficar abaixo de -5 pontos?
				
			- GET /recommendations/top/:amount
				- Pega x recomendações mais votadas?
				- Exibe sua pontuação
			- GET /recommendations/random
				- Retorna recomendação no formato proposto?
				- A recomendação é aleatória de acordo com a regra definida?
			- Bônus: POST /genres
				
			- Bonus: GET /genres
				
		- Avaliação por requisito
			- **POST /recommendations**: Requisitos entregues totalmente
			- **POST /recommendations**: Requisitos entregues totalmente
			- **POST /recommendations/:id/upvote ou downvote**: Requisitos entregues totalmente
			- **POST /recommendations/:id/downvote: Exclusão**: Requisitos entregues totalmente
			- **GET /recommendations/top/:amount**: Requisitos entregues totalmente
			- **GET /recommendations/random**: Requisitos entregues totalmente
			- **Bônus: POST /genres**: Requisitos entregues totalmente
			- **Bonus: GET /genres**: Requisitos entregues totalmente
			- **Bonus: GET /genres/:id**: Requisitos não entregues
			- **GET /recommendations/genres/:id/random**: Requisitos não entregues
			- **Bônus: Adicionar dados dos gêneros nas rotas de recommendations**: Requisitos não entregues
			- **Express Router**: Requisitos entregues totalmente
	- **Feedback de Código**
		- Feedback Qualitativo: disponível no Pull Request no GitHub (Pode fechar o Pull Request depois de ler, clicando em Close Pull Request.) "